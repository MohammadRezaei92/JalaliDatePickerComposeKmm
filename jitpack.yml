jdk:
  - openjdk17

build:
  commands:
    # Run the Gradle tasks to zip the iOS frameworks
    - ./gradlew clean
    - ./gradlew zipIosArm64Framework
    - ./gradlew zipIosX64Framework
    - ./gradlew zipIosSimulatorArm64Framework